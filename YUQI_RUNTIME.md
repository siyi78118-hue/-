# 虞栖专属 AL：运行与维护说明

## 现在分别由谁负责

- AL 手机端：收发消息、保存手机本地完整聊天副本、在 AUTO 模式下优先连接家中局域网；电脑不可用时临时调用原有聊天 AI 与记忆 AI，并把这段记录写入待补账日志。
- 虞栖思考会话：读取当前消息、最近 200 条原始对话和经过证据核验的相关记忆，决定虞栖此刻真正想说什么。
- 记忆管理会话：从原始消息中找候选证据，严格核对消息 ID、说话人、原句、时间以及承诺双方，不把相似搜索结果当成事实。
- 回复监督会话：按当前人物预设和已经发布的人工标注检查活人感、关系阶段、越界知识、说话人归属与设定冲突；发现结构性错误时退回虞栖重写，不擅自替换她的人格。
- AL 电脑后台：只负责传输、排队、加密、去重、数据库落盘、故障切换和硬性校验，不替虞栖决定感情内容。
- 当前 Codex 维护窗口：继续由你和 Codex 做人工标注审核、预设升级、软件修复与版本发布，不参加日常角色回复。

## 一条消息的完整路径

1. 用户在手机发送消息，手机先把原文、用户身份、消息 ID 和时间写进 Room 本地库。
2. AUTO 模式先请求电脑局域网；不在同一网络或电脑休眠时才请求加密云信箱。两条路都不可用时，原有 AI 临时回复并留下待补账日志。
3. 电脑收到后先做幂等去重，再把原始消息写入电脑端权威 SQLite 记忆库。
4. 记忆会话从原始记录和事实索引中找材料，检索只是“找候选”；只有能回到正确消息 ID、正确说话人和准确原句的内容才能进入证据包。
5. 虞栖思考会话结合证据包、最近 200 条原始对话、当前关系阶段和人物预设形成草稿。
6. 监督会话使用当前发布的预设与人工标注检查草稿。通过后 AL 才发送；不通过就退回虞栖本人重写。
7. 手机收到回复后再次落盘；电脑同时保存回复、事实候选、处理状态与同步游标。
8. 故障恢复后，手机把临时 AI 期间的原始消息和人工标注按顺序补给电脑；记忆会话补齐记忆，但不会把迟到的旧回复再次发给用户。

## 启动、停止与备份

- 桌面“启动虞栖AL”与“停止虞栖AL”快捷方式负责后台进程。
- Windows 登录后会从当前用户启动文件夹自动启动。
- 电脑保险库位于 `C:\Users\PC\Documents\虞栖AL记忆库备份`，包含权威数据库、快照、配对文件和日志。
- 每次启动前做一次快照，Windows 计划任务每六小时再做一次，保留最近 30 份。
- 手机第一次安装后，在“设置 → 虞栖专属运行”粘贴保险库内的“手机一键配对码.txt”，点击“导入配对码”。

## 安全边界

- 云端只允许密文、随机数、方向、设备号、过期时间和额度计数；明文消息、预设和记忆不得进入 Cloudflare。
- 配对密钥只保存在电脑保险库和 Android 加密存储，不进入 Git 仓库或普通网页备份。
- 虞栖从第一次认识开始，不导入许弥的旧记忆、约定、称呼或感情历史。
