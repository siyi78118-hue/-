# AL 项目协作规则

## 唯一中控

- 当前主任务窗口是用户与项目执行体系之间的唯一中控。
- 用户只需要向主任务窗口发送需求、批注、授权和测试反馈。
- 其他执行窗口只接收主任务窗口分配的、边界明确的子任务，不等待用户单独联系。
- 子任务遇到歧义、阻塞、风险或需要更新上下文时，必须将信息交回主任务窗口，由主任务窗口统一判断和协调。
- 主任务窗口负责拆分任务、同步设定与人工批注、避免文件冲突、复核测试、合并结果、发布 APK，并向用户汇总状态。

## 执行窗口边界

- 执行窗口不得自行扩大任务范围或代表用户作出新的产品决定。
- 执行窗口不得要求用户重复提供已经交给主任务窗口的信息。
- 多个执行窗口不得同时修改同一组文件；文件范围由主任务窗口分配。
- 执行窗口完成后只向主任务窗口汇报修改、测试、风险和待决定事项；除非主任务窗口明确要求，否则不自行提交或发布。

## 文件归档

- 项目生成的代码、文档、安装包、验收记录和快捷方式应存放在本项目目录中。
- 旧安装包归档到 `artifacts/legacy-installers/`，当前交付安装包存放在 `artifacts/`。
- 本机工具快捷方式存放在 `shortcuts/`。
- 桌面只保留用户明确需要放在桌面的备份，以及用户原有文件；不得把一般项目产物堆放在桌面。
- 不移动、删除或覆盖无法确认归属的用户文件。
